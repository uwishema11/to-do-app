export interface completeType {
  id: string;
  complete: boolean;
}
export interface todoType {
  id: string;
  title: string;
  complete: boolean;
  userId: string;
}
export interface taskType {
  title: string;
}